Today, I worked on fixing both of the websites. Both of them were in a kind of broken state, and I had to debug them manually. I started by cleaning up a lot of the code for the battle spirits database. There was a lot of redundant code, and I removed it. I also fixed a lot of the api endpoints that were completely broken due to the client and server having different protocols. The delete and edit function were completely broken. I also fixed a bug related to deleting cards not causing the component to refresh, resulting in ghost cards. Refreshing the whole component is expensive though, so instead I filter out cards that are "deleted" until we have to actually refresh. There still are a couple of broken features:
- Editing doesn't properly change the frontend UI. We could refresh the whole component, but this wouldn't be ideal. We also can't just hide the cards like the deletion feature. I will need to brainstorm this one more.
- Deckbuilding is still broken.