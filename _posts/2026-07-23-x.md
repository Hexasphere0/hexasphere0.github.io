Today, I worked on fixing up a lot of things on the website. I completely reworked the interface. Now, the previous card list where you selected cards for printing is gone. Instead, you create a deck, add cards to it (which show up as a list), and then can export the whole deck for printing. I think this interface is way better, but it still needs work.