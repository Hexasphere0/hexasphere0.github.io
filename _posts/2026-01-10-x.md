I tried to change the code to use Unity's new input system. I created an input map and left it at the default actions, which included a configuration for movement. I should be able to create a script and reference the movement action in order to get the direction the player is traveling. However, when I implemented this, it didn't work. I searched up possible issues and I eliminated everything that could be related to my code. I finally figured out that there was an input debugger in unity, that showed when events are triggered. After opening this, I realized that the input actions I defined were simply not registered at all. After searching this up, I figured out that I could re-enable reloading domain and scene in the editor. This makes entering play mode slower, but fixes the issue.